spring:
  application:
    name: distributed-coupon-system
  datasource:
    url: jdbc:postgresql://localhost:5432/coupon_db
    username: postgres
    password: postgres

server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: "*"